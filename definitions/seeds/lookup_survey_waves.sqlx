config {
    type: "table",
}

SELECT
  *
FROM
  UNNEST(
    -- Define an array of structs. Each struct is a row in your table.
    ARRAY<STRUCT<name STRING, provider STRING, start_date DATE, end_date DATE>>[ ('wave 1',
      'BMG',
      NULL,
      NULL) ] )
